{
  "name": "data-replicator",
  "version": "1.0.0",
  "description": "A simple replicator system that obtain the data from different resources and store it in different caches, improving the performance of the whole service",
  "main": "dist/index.js",
  "scripts": {
    "start": "npx ts-node index.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alexcabezas0",
  "license": "MIT",
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.7.3"
  }
}
