stores:
  - name: users
    type: http
    refreshInterval: 5000
    mapper:
      class: User
      key: id
    indexes:
      - key: bySurname
        field: surname
    config:
      url: http://localhost:3000/users